% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formatArguments.R
\name{getCovDataTable}
\alias{getCovDataTable}
\title{Convert Covariates to One-Hot Encoded Data Table}
\usage{
getCovDataTable(
  DataTable,
  EventTime,
  EventType,
  Treatment,
  ID,
  LongTime,
  Verbose
)
}
\arguments{
\item{DataTable}{data.table containing all observed data}

\item{EventTime}{character; column name of event time (excluded from covariates)}

\item{EventType}{character; column name of event type (excluded from covariates)}

\item{Treatment}{character; column name(s) of treatment (excluded from covariates)}

\item{ID}{character; column name of subject ID (excluded from covariates)}

\item{LongTime}{character; column name of longitudinal time (excluded, currently unused)}

\item{Verbose}{logical; whether to print diagnostic information}
}
\value{
data.table with one-hot encoded covariates. Has attribute "CovNames"
containing a mapping table with columns:
\itemize{
\item ColName: standardized column name ("L1", "L2", ...)
\item CovName: original covariate name
\item CovVal: factor level (for categorical) or "." (for numeric)
}
}
\description{
Transforms covariate data into a standardized format with one-hot encoding
for categorical variables. Numeric variables are preserved as-is. Column
names are standardized to "L1", "L2", etc. for consistent downstream processing.
}
\details{
Uses vectorized model.matrix() for efficient one-hot encoding of all
categorical variables simultaneously, rather than processing each column
in a loop. This provides better performance for datasets with many
categorical covariates.
}
\keyword{internal}
