% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getEIC.R
\name{getIC}
\alias{getIC}
\title{Compute Individual-Level Efficient Influence Curves}
\usage{
getIC(
  GStar,
  Hazards,
  TotalSurv,
  NuisanceWeight,
  TargetEvent,
  TargetTime,
  T.tilde,
  Delta,
  EvalTimes,
  GComp
)
}
\arguments{
\item{GStar}{numeric vector; intervention probabilities g*(A|W) for each subject}

\item{Hazards}{named list of hazard matrices (times x subjects) for each event type}

\item{TotalSurv}{matrix (times x subjects); overall survival probability S(t)}

\item{NuisanceWeight}{matrix (times x subjects); weights combining g(A|W) and
censoring survival}

\item{TargetEvent}{numeric vector; event types to compute IC for}

\item{TargetTime}{numeric vector; time points to compute IC at}

\item{T.tilde}{numeric vector; observed event/censoring times per subject}

\item{Delta}{numeric vector; observed event types per subject (0=censored)}

\item{EvalTimes}{numeric vector; all evaluation time points}

\item{GComp}{logical; whether g-computation is being performed (for consistency)}
}
\value{
data.table with columns ID, Time, Event, IC containing the efficient
influence curve value for each subject-time-event combination
}
\description{
Computes the efficient influence curve (EIC) for each subject, target event,
and target time. This is the core computation for TMLE variance estimation.
}
\details{
The function uses matrix operations with strategic caching to avoid redundant
subsetting operations. For each target time tau, the following are pre-computed:
\itemize{
\item Logical index for times <= tau
\item Subset of cumulative incidence function F(t)
\item Subset of survival function S(t)
\item Subset of nuisance weights
}
}
\keyword{internal}
