% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doTmleUpdate.R
\name{updateHazard}
\alias{updateHazard}
\title{Update Hazards Along Least Favorable Submodel (R Implementation)}
\usage{
updateHazard(
  GStar,
  Hazards,
  TotalSurv,
  NuisanceWeight,
  EvalTimes,
  T.tilde,
  Delta,
  PnEIC,
  NormPnEIC,
  OneStepEps,
  TargetEvent,
  TargetTime,
  Iterative = FALSE
)
}
\arguments{
\item{GStar}{numeric vector; intervention probabilities g*(A|W) for each subject}

\item{Hazards}{list; cause-specific hazard matrices (times x subjects)}

\item{TotalSurv}{matrix; overall survival S(t) (times x subjects)}

\item{NuisanceWeight}{matrix; inverse of g-related weights (times x subjects)}

\item{EvalTimes}{numeric vector; time points for hazard evaluation}

\item{T.tilde}{numeric vector; observed event/censoring times}

\item{Delta}{numeric vector; observed event types (0 = censored)}

\item{PnEIC}{data.table; empirical mean EIC with columns Time, Event, PnEIC}

\item{NormPnEIC}{numeric; current ||PnEIC|| norm for scaling updates}

\item{OneStepEps}{numeric; step size multiplier}

\item{TargetEvent}{numeric vector; event types being targeted}

\item{TargetTime}{numeric vector; time points being targeted}
}
\value{
list of updated hazard matrices with same structure as input Hazards
}
\description{
Computes the one-step TMLE update of cause-specific hazards. This is the R
reference implementation; the C++ version (updateHazardsCpp) is used in production.
}
\details{
For each cause l, the hazard update is:
h_l,new(t) = h_l,old(t) * exp(eps / ||PnEIC|| * sum_\{j,tau\} H_\{l,j,tau\}(t) * PnEIC_\{j,tau\})

where H_\{l,j,tau\} is the clever covariate measuring the sensitivity of the
risk F_j(tau) to perturbations in hazard h_l.
}
\keyword{internal}
